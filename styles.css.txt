* { box-sizing: border-box; }
html, body { margin:0; height:100%; background:#0b1220; color:#e7f1ff; font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, Arial; }
canvas { display:block; }

#ui {
  position: fixed; left: 12px; top: 12px; z-index: 10;
  background: rgba(0,0,0,.35);
  border: 1px solid rgba(255,255,255,.15);
  padding: 10px 12px; border-radius: 12px; backdrop-filter: blur(6px);
  box-shadow: 0 10px 24px rgba(0,0,0,.2);
  max-width: 420px;
}
#ui h1 { margin:0 0 6px; font-size: 16px; letter-spacing: .2px; }
#ui .row { display:flex; gap:14px; align-items:center; margin-top:6px; font-size:13px; opacity:.95; }
#hint { margin-top: 6px; font-size: 12.5px; opacity: .9; line-height: 1.45; }
#ui .buttons { display:flex; gap:8px; margin-top:8px; }
#ui button {
  border: 1px solid rgba(255,255,255,.2);
  background: rgba(255,255,255,.08);
  color: #e7f1ff; cursor: pointer; border-radius: 10px;
  padding: 8px 12px; font-size: 13px;
}
#ui button:hover { background: rgba(255,255,255,.16); }

#minimap {
  position: fixed; right: 12px; bottom: 12px; z-index: 9;
  border-radius: 12px; border: 1px solid rgba(255,255,255,.15);
  background: rgba(0,0,0,.32); backdrop-filter: blur(6px);
  box-shadow: 0 10px 24px rgba(0,0,0,.25);
}

#gameOver {
  position: fixed; inset: 0; z-index: 20; display: grid; place-items: center;
  background: rgba(0,0,0,.55); backdrop-filter: blur(4px);
}
.hidden { display: none; }
#gameOver .panel {
  background: rgba(12,18,32,.9);
  border: 1px solid rgba(255,255,255,.18);
  border-radius: 16px; padding: 18px 22px; text-align: center; min-width: 260px;
  box-shadow: 0 20px 40px rgba(0,0,0,.35);
}
#gameOver .panel h2 { margin: 0 0 8px; }
#gameOver .panel button {
  margin-top: 10px; padding: 10px 14px; border-radius: 10px;
  border: 1px solid rgba(255,255,255,.25);
  background: rgba(255,255,255,.12); color: #e7f1ff; cursor:pointer;
}
#gameOver .panel button:hover { background: rgba(255,255,255,.22); }

/* make room for UI on tiny screens */
@media (max-width: 520px) {
  #ui { max-width: 92vw; left: 8px; right: 8px; }
  #minimap { width: 140px; height: 140px; }
}
